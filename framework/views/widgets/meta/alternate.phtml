<?php 

if ($this->extra->isUriMultiLang()) {
    $langs = $this->config->langs;
    $c_lang = $this->config->lang;

    foreach ($langs as $lang) {
		if ($lang == $c_lang) continue;
    ?>
<link rel="alternate" href="<?='http'.(isset($_SERVER['HTTPS'])?'s':'').'://'.$_SERVER['HTTP_HOST'].$this->extra->getUriInLang($lang)?>" hreflang="<?=strtolower($lang)?>" />
	<?php 
	}
} 
?>

